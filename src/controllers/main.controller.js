const indexController = require('../controllers/index.controller');
const artistsController = require('../controllers/artistas.controller');

module.exports = {
    indexController,
    artistsController
}